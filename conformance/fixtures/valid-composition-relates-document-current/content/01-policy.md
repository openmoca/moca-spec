# API Usage Policy

This is the current, standalone policy document. It cross-references a
separate rate-limit reference document and supersedes an earlier version of
itself, both expressed via `composition.relates` (core §10.2) rather than
containment — neither related document is "part of" this one.
