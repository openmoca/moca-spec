# Introduction

This package demonstrates the minimum Level 1 MOCA format: a valid manifest
and ordinary CommonMark content. It requires no YAML frontmatter, concept
binding, semantic processing, or runtime service.
