---
name: example-skill
description: Performs a defined task against package concepts.
metadata:
  concepts: [ex:ServiceCommunication]
allowed-tools: [read_file, grep_search]
---
# Skill Instructions

This is a placeholder Agent Skill demonstrating the `skills/` convention
from [core §8.1](../../../../moca-core-spec.md#81-agent-skills-integration).
It does not implement real logic — it exists so this example package has
something for the mandatory `signature` in `moca.json` to cover.
